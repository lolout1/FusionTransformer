\section{Results}
\label{sec:Results}

We present comprehensive experimental results evaluating the impact of Kalman fusion on fall detection performance. All experiments use 21-fold Leave-One-Subject-Out Cross-Validation (LOSO-CV) with young subjects as test folds and elderly subjects contributing ADL samples for training. We report F1 score as the primary metric given the class imbalance inherent in fall detection (falls are rare events). Each experiment was conducted with a fixed random seed for reproducibility.

\subsection{Kalman Fusion Ablation Study}
\label{sec:kalman_ablation}

To isolate the effect of Kalman fusion, we conducted a controlled comparison using identical model architecture, hyperparameters, and training procedures. The only variable changed was whether the input was processed through our linear Kalman filter to extract orientation features (roll, pitch, yaw) or used as raw accelerometer and gyroscope signals.

\begin{table}[htbp]
\centering
\caption{Controlled Comparison: Kalman Fusion vs.\ Raw Input (21-fold LOSO-CV). Both conditions use identical dual-stream transformer with SE attention, TAP, 50:50 embedding ratio, and focal loss ($\alpha=0.75$, $\gamma=2.0$).}
\label{tab:kalman_vs_raw}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{@{}lcccc@{}}
\toprule
\textbf{Input Type} & \textbf{F1 (\%)}  & \textbf{Acc (\%)} & \textbf{Prec (\%)} & \textbf{Recall (\%)} \\
\midrule
Raw 7ch & 89.49 $\pm$ 6.4 & 84.90 $\pm$ 8.7 & 86.77 $\pm$ 8.8 & 93.13 $\pm$ 7.6 \\
\textbf{Kalman 7ch} & \textbf{90.89 $\pm$ 5.8} & \textbf{86.98 $\pm$ 7.3} & \textbf{88.33 $\pm$ 8.2} & \textbf{94.38 $\pm$ 7.5} \\
\midrule
$\Delta$ & \textbf{+1.40} & +2.08 & +1.56 & +1.25 \\
\bottomrule
\end{tabular}%
}
\vspace{1mm}
\scriptsize
\textit{Raw 7ch: acc$_{xyz}$ + gyro$_{xyz}$ + SMV. Kalman 7ch: acc$_{xyz}$ + roll, pitch, yaw + SMV.}
\end{table}

Table~\ref{tab:kalman_vs_raw} demonstrates that Kalman fusion consistently improves all metrics. The F1 score improvement of +1.40\% (from 89.49\% to 90.89\%) is achieved by replacing raw gyroscope channels with Kalman-estimated orientation angles. Notably, the standard deviation is also reduced with Kalman fusion (5.80\% vs 6.44\%), indicating more consistent performance across subjects.

\begin{figure}[htbp]
\centering
\includegraphics[width=\columnwidth]{figures/training_curves_kalman_se_tap.png}
\caption{Training curves for the best model (Kalman + SE + TAP). Left: Loss curves showing train/validation convergence. Right: F1 score progression across epochs, averaged over 21 LOSO folds.}
\label{fig:training_curves}
\end{figure}

The Kalman-fused input representation consists of 7 channels: signal magnitude vector (SMV), accelerometer components ($a_x$, $a_y$, $a_z$), and orientation angles (roll, pitch, yaw). In contrast, the raw representation uses accelerometer ($a_x$, $a_y$, $a_z$), gyroscope ($\omega_x$, $\omega_y$, $\omega_z$), and SMV. The orientation representation provides a more interpretable and physically meaningful feature space for fall detection, as falls are characterized by specific orientation changes rather than raw angular velocities.

\subsection{SE and TAP Ablation Study}
\label{sec:se_tap_ablation}

To understand the contribution of Squeeze-Excitation (SE) channel attention and Temporal Attention Pooling (TAP) modules, we conducted a 2$\times$2 factorial ablation study on both raw and Kalman-fused inputs.

\begin{table}[htbp]
\centering
\caption{SE/TAP Ablation: F1 Score (\%) for Raw vs Kalman Input (21-fold LOSO-CV).}
\label{tab:se_tap_ablation}
\begin{tabular}{@{}lcccc@{}}
\toprule
\textbf{Config} & \textbf{SE} & \textbf{TAP} & \textbf{Raw} & \textbf{Kalman} \\
\midrule
A & No & No & 89.20 & 89.63 \\
B & Yes & No & 90.66 & 90.07 \\
C & No & Yes & 90.54 & 89.01 \\
D & Yes & Yes & 89.08 & \textbf{90.85} \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[htbp]
\centering
\includegraphics[width=\columnwidth]{figures/ablation_heatmap.png}
\caption{SE/TAP ablation heatmap comparing Raw (left) vs Kalman (right) input. Kalman fusion benefits most when both SE and TAP are enabled (+1.77\% over Raw).}
\label{fig:ablation_heatmap}
\end{figure}

Table~\ref{tab:se_tap_ablation} and Figure~\ref{fig:ablation_heatmap} reveal an important interaction effect: while SE and TAP individually improve performance on raw input, their combination yields the best results only with Kalman fusion (90.85\%). This suggests that the cleaner orientation signals from Kalman filtering enable the attention mechanisms to more effectively focus on fall-relevant temporal patterns.

\subsection{Architecture Comparison: Transformer vs CNN}
\label{sec:arch_comparison}

To validate that Kalman fusion benefits are not architecture-specific, we compared our transformer-based approach with a CNN-Mamba hybrid architecture.

\begin{table}[htbp]
\centering
\caption{CNN-Mamba Input Comparison (21-fold LOSO-CV).}
\label{tab:cnn_comparison}
\begin{tabular}{@{}lc@{}}
\toprule
\textbf{CNN-Mamba Input} & \textbf{F1 (\%)} \\
\midrule
Raw 6ch (acc + gyro) & \textbf{89.98} \\
Kalman 6ch (acc + ori) & 88.09 \\
\midrule
$\Delta$ & -1.89 \\
\bottomrule
\end{tabular}
\end{table}

Interestingly, Table~\ref{tab:cnn_comparison} shows that the CNN-Mamba architecture performs \textit{better} with raw input (89.98\%) than Kalman-fused input (88.09\%). This contrasts with the transformer, which benefits significantly from Kalman fusion. The CNN's convolutional layers may be better suited to extract patterns from high-frequency raw signals, while transformers benefit more from the cleaner orientation representation.

\begin{figure}[htbp]
\centering
\includegraphics[width=\columnwidth]{figures/f1_bar_comparison.png}
\caption{F1 score comparison across model configurations. Kalman fusion with SE+TAP achieves the best performance (90.85\%).}
\label{fig:f1_comparison}
\end{figure}

\begin{table}[htbp]
\centering
\caption{Transformer Architecture Comparison (21-fold LOSO-CV).}
\label{tab:transformer_arch}
\begin{tabular}{@{}lcc@{}}
\toprule
\textbf{Model} & \textbf{Stream} & \textbf{F1 (\%)} \\
\midrule
TransModel & Single & 86.99 \\
TransModelSE & Single + SE/TAP & 87.80 \\
\textbf{KalmanDualStream} & Dual + SE/TAP & \textbf{90.85} \\
\bottomrule
\end{tabular}
\end{table}

Table~\ref{tab:transformer_arch} demonstrates that the dual-stream architecture with Kalman fusion significantly outperforms single-stream alternatives. The +2.87\% improvement from TransModelSE to KalmanDualStream validates our hypothesis that separate processing of accelerometer and orientation streams enables more effective feature extraction.

\subsection{Loss Function Analysis}
\label{sec:loss_analysis}

Given the severe class imbalance in fall detection (falls represent a minority of samples), we investigated the impact of focal loss versus binary cross-entropy (BCE) loss.

\begin{table}[htbp]
\centering
\caption{Loss Function Comparison on CNN-Mamba Architecture (21-fold LOSO-CV).}
\label{tab:loss_comparison}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{@{}lcccc@{}}
\toprule
\textbf{Loss Function} & \textbf{F1 (\%)} & \textbf{Acc (\%)} & \textbf{Prec (\%)} & \textbf{Recall (\%)} \\
\midrule
BCE & 88.01 $\pm$ 8.4 & 84.26 $\pm$ 10.0 & \textbf{92.17 $\pm$ 7.5} & 85.70 $\pm$ 13.6 \\
\textbf{Focal} & \textbf{88.86 $\pm$ 6.8} & 84.22 $\pm$ 8.2 & 86.79 $\pm$ 9.4 & \textbf{92.11 $\pm$ 8.9} \\
\midrule
$\Delta$ & \textbf{+0.85} & -0.04 & -5.38 & \textbf{+6.41} \\
\bottomrule
\end{tabular}%
}
\vspace{1mm}
\scriptsize
\textit{Focal loss: $\alpha=0.75$, $\gamma=2.0$}
\end{table}

Table~\ref{tab:loss_comparison} reveals that focal loss improves F1 score by +0.85\% primarily through a substantial increase in recall (+6.41\%). This is clinically significant for fall detection systems, where missing a fall (false negative) has more severe consequences than a false alarm. The focal loss down-weights easy negatives (ADL samples correctly classified with high confidence), allowing the model to focus learning on the harder fall detection task. While precision decreases with focal loss, the overall F1 score improves due to the recall gains.

\subsection{Per-Subject Analysis}
\label{sec:subject_analysis}

To understand the robustness of our approach, we analyzed performance variation across individual test subjects (Figure~\ref{fig:critical_subjects}).

\begin{figure}[htbp]
\centering
\includegraphics[width=\columnwidth]{figures/critical_subjects_analysis.png}
\caption{Critical subject analysis showing worst 3 (left) and best 3 (right) subjects across architectures. Kalman fusion provides substantial improvement for challenging subjects (S36: +23.7\%, S38: +6.6\%) while maintaining performance on high-performing subjects.}
\label{fig:critical_subjects}
\end{figure}

Our analysis reveals that Kalman fusion particularly benefits challenging subjects whose fall patterns are difficult to distinguish from ADLs. Subject 36, the most challenging case, improved from 66.67\% to 90.32\% F1 (+23.65\%) with Kalman fusion. This suggests that orientation features provide discriminative information when accelerometer-based patterns are ambiguous.

\begin{figure}[htbp]
\centering
\includegraphics[width=\columnwidth]{figures/training_dynamics_analysis.png}
\caption{Training dynamics showing loss convergence for different configurations. All models converge smoothly with minimal overfitting, as indicated by small train-validation gaps.}
\label{fig:training_dynamics}
\end{figure}

\subsection{Summary of Key Findings}

Our experimental evaluation leads to the following conclusions:

\begin{enumerate}
    \item \textbf{Kalman fusion improves transformer-based fall detection}: Under controlled conditions with identical architecture, Kalman-fused orientation features outperform raw gyroscope data by +1.40\% F1 score (90.89\% vs 89.49\%) for transformers.

    \item \textbf{SE and TAP synergy with Kalman fusion}: The combination of SE attention and TAP pooling provides the greatest benefit when used with Kalman fusion (+1.77\% over raw with SE+TAP: 90.85\% vs 89.08\%).

    \item \textbf{Dual-stream architecture is superior}: The dual-stream transformer (86.99\% $\rightarrow$ 90.85\%) significantly outperforms single-stream alternatives (+3.86\%), validating separate processing of accelerometer and orientation streams.

    \item \textbf{Architecture-specific input preference}: While transformers benefit from Kalman fusion, CNN-Mamba performs better with raw input (89.98\% vs 88.09\%), suggesting CNNs are better suited for high-frequency raw signal processing.

    \item \textbf{Focal loss benefits imbalanced classification}: Focal loss improves recall by +6.41\% compared to BCE, which is critical for fall detection where missing falls has severe consequences.

    \item \textbf{Best configuration achieves 90.85\% F1}: The optimal configuration combines Kalman fusion, dual-stream transformer architecture with SE attention and temporal attention pooling, 50:50 embedding ratio, and focal loss, achieving 90.85\% F1 score on 21-fold LOSO-CV.
\end{enumerate}

