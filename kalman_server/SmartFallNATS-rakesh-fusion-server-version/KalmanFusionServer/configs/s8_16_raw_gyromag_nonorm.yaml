# s8_16_raw_gyromag_nonorm - F1: 87.49% +/- 9.53
# Best fold: S45 (99.8%), Trained: 2026-02-04
# Architecture: SingleStreamTransformerSE (single-stream)
# Input: Raw gyro magnitude [smv, ax, ay, az, gyro_mag] - 5 channels
# No normalization

nats_url: "nats://localhost:4222"
subject_pattern: "m.kalman_transformer.*"
default_fs_hz: 31.25
window_size: 128

preprocessing:
  feature_mode: raw_gyromag
  normalization_mode: none
  scaler_path: null
  convert_gyro_to_rad: true

kalman:
  filter_type: linear
  Q_orientation: 0.005
  Q_rate: 0.01
  R_acc: 0.05
  R_gyro: 0.1

state:
  timeout_ms: 10000
  cache_ttl_ms: 60000
  enable_incremental: false

model:
  architecture: SingleStreamTransformerSE
  weights_path: weights/s8_16_raw_gyromag_nonorm.pth
  device: cpu
  model_args:
    imu_frames: 128
    imu_channels: 5
    embed_dim: 48
    acc_ratio: 0.65
    num_heads: 4
    num_layers: 2
