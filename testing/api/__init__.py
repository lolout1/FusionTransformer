"""FastAPI endpoints (future).

To use FastAPI instead of Streamlit:

1. Install: pip install fastapi uvicorn
2. Run: uvicorn testing.api.main:app --host 0.0.0.0 --port 8000

The API uses the same service layer as Streamlit, ensuring consistent behavior.
"""
